name: Skip CI

on:
  push:
    branches:
      - '**'
  pull_request:
    branches:
      - '**'

jobs:
  skip:
    runs-on: ubuntu-latest
    steps:
      - name: 🚫 Skip all actions
        run: |
          echo "🧱 No build, no test, no install — this repo is for storage only."
