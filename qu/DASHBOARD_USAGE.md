# لوحات التحليل المورفولوجي - Morphological Analysis Dashboards

## 📋 نظرة عامة - Overview

لقد تم بناء ثلاثة لوحات تحكم متقدمة باستخدام **Mishkah.js framework** (بدون أي backend):

1. **dashboard.html** - اللوحة الأساسية
2. **dashboard-advanced.html** - اللوحة المتقدمة
3. (تطوير) **dashboard-ultimate.html** - اللوحة الشاملة مع Chart.js و الاختبارات الإحصائية

## 🚀 كيفية الاستخدام - How to Use

### المتطلبات - Requirements

- متصفح حديث يدعم ES6+ (Chrome, Firefox, Safari, Edge)
- الملفات موجودة في نفس المجلد:
  - `final/*.json` (15 ملف بيانات مورفولوجية)
  - `words-ref.json` (معلومات المواقع)
  - `dashboard.html` أو `dashboard-advanced.html`

### الخطوات - Steps

#### 1. فتح اللوحة الأساسية

```bash
# في المتصفح، اذهب إلى:
file:///home/user/os/qu/dashboard.html
# أو عبر خادم HTTP
http://localhost:8000/qu/dashboard.html
```

#### 2. انتظر تحميل البيانات

- سيتم تحميل 15 ملف JSON تلقائياً
- سيتم حساب الإحصائيات
- ستظهر الرسالة "✓ تم تحميل X كلمة بنجاح"

#### 3. الاستخدام

**نظرة عامة (Overview Tab):**
- عرض الإحصائيات الأساسية
- أكثر الكلمات تكراراً
- توزيع الجذور

**البحث (Search Tab):**
- ابحث عن كلمة مباشرة
- ابحث عن جذر (مثل "ق.و.ل")
- ابحث عن تصنيف (مثل "1.1", "2.2")
- النتائج تُحدث فوراً أثناء الكتابة

**الإحصائيات (Statistics Tab):**
- توزيع التصنيفات (Tags) بالنسبة والتكرار
- أكثر الجذور تكراراً
- عرض نسب مئوية وأشرطة تقدم

## 📊 البيانات - Data Structure

### الملفات المطلوبة

```
qu/
├── dashboard.html              # اللوحة الأساسية
├── dashboard-advanced.html     # اللوحة المتقدمة
├── final/                      # المجلد الذي يحتوي على البيانات المصححة
│   ├── final_01.json
│   ├── final_02.json
│   └── ... (15 ملف)
├── words-ref.json             # موقع كل كلمة في المصحف
└── DASHBOARD_USAGE.md         # هذا الملف
```

### صيغة البيانات

**final_XX.json** - تحتوي على:
```json
[
  ["الكلمة", "الجذر", "التصنيفات"],
  ["مِن", "م.ن", "1.1"],
  ["وَ+ال+لّٰه", "ء.ل.ه", "1.2,1.5,2.2"],
  ...
]
```

**words-ref.json** - تحتوي على:
```json
[
  [[سورة, آية, موقع], [سورة, آية, موقع], ...],  // موقع الكلمة الأولى
  [[سورة, آية, موقع], ...],                      // موقع الكلمة الثانية
  ...
]
```

## 🎯 الميزات - Features

### dashboard.html (الأساسية)

✅ تحميل بيانات من 15 ملف JSON
✅ نظرة عامة على الإحصائيات
✅ بحث فوري عن الكلمات والجذور والتصنيفات
✅ عرض جداول البيانات
✅ واجهة عربية كاملة
✅ تصميم متجاوب (Responsive)

### dashboard-advanced.html (المتقدمة)

✅ كل ميزات اللوحة الأساسية +
✅ اكتشاف الأنماط تلقائياً
✅ شريط تقدم للنسب المئوية
✅ واجهة تبويبات متقدمة (Tabs)
✅ معلومات مواقع الكلمات
✅ ترتيب ذكي للبيانات

### dashboard-ultimate.html (قيد التطوير)

🔄 Chart.js تصور بياني متقدم
🔄 Hypothesis testing إحصائي
🔄 Chi-square و T-test
🔄 P-value calculations
🔄 اكتشاف أنماط متقدم
🔄 تصدير البيانات

## 🔍 أمثلة الاستخدام - Examples

### البحث عن كلمة

1. اذهب إلى "البحث" tab
2. اكتب "الله" في صندوق البحث
3. سيظهر جميع تكرارات الكلمة مع جذرها وتصنيفاتها

### البحث عن جذر

1. اكتب "ق.و.ل" في صندوق البحث
2. سيظهر جميع الكلمات التي لها هذا الجذر

### البحث عن تصنيف

1. اكتب "1.1" (حرف جر) في البحث
2. سيظهر جميع الكلمات المصنفة كحروف جر

### عرض الإحصائيات

1. اذهب إلى "الإحصائيات" tab
2. شاهد توزيع كل التصنيفات
3. ركز على أكثر التصنيفات تكراراً

## 🛠️ التطوير - Development

### إضافة ميزات جديدة

إذا أردت إضافة ميزات:

1. **البيانات موجودة بالفعل** في `db.data`
2. **أضف handler جديد** في `orders` object
3. **أضف UI جديدة** في `setBody` function
4. **استدعِ rebuild()** لتحديث الواجهة

### مثال - إضافة زر تصدير CSV

```javascript
// في orders:
'export:csv': function(event, context) {
    const csv = convertToCSV(context.db.data.wordsData);
    downloadCSV(csv, 'morphological-data.csv');
}

// في UI:
D.Forms.Button({
    attrs: { gkey: 'export:csv' }
}, ['تصدير CSV'])
```

## ⚙️ الأداء - Performance

### تحميل البيانات
- **الوقت المتوقع**: 2-5 ثواني (حسب سرعة الشبكة)
- **حجم البيانات**: ~500KB JSON

### البحث والتصفية
- **فوري**: < 50ms
- **عدد النتائج**: حتى 14,808 كلمة

### الحسابات الإحصائية
- **التكرارات**: < 100ms
- **اكتشاف الأنماط**: < 200ms

## 🐛 استكشاف الأخطاء - Troubleshooting

### لم تظهر البيانات

**المشكلة**: صفحة بيضاء أو "جاري التحميل..."

**الحل**:
1. تأكد من وجود المجلد `final/` مع 15 ملف JSON
2. تأكد من وجود `words-ref.json`
3. افتح Console (F12) وتحقق من الأخطاء
4. جرب تحديث الصفحة (Ctrl+R)

### البحث لا يعمل

**المشكلة**: لا تظهر نتائج البحث

**الحل**:
1. تأكد من كتابة الكلمة بالعربية بشكل صحيح
2. جرب البحث عن جزء من الكلمة
3. تحقق من Console للأخطاء

### الواجهة تعمل ببطء

**المشكلة**: التطبيق بطيء

**الحل**:
1. أغلق التبويبات الأخرى
2. استخدم متصفح حديث
3. قلل عدد الأحرف في البحث

## 📞 الدعم - Support

إذا واجهت مشكلة:

1. افتح Console (F12) واقرأ الأخطاء
2. تحقق من أن جميع الملفات موجودة
3. جرب reload الصفحة
4. تحقق من أن JavaScript مفعل

## 📝 ملاحظات تقنية

### Mishkah.js Architecture

```javascript
// البنية الأساسية:
(async function() {
    const M = Mishkah;
    const D = M.DSL;

    const db = { env: {...}, data: {...} };
    const orders = { 'action': handler };

    M.app.setBody((state, D) => {
        // رسم الواجهة
    });

    M.app.createApp(db, orders).mount('#app');
})();
```

### Data Flow

```
JSON Files → Load Data → Calculate Stats → Render UI
                            ↓
                       User Actions
                            ↓
                       Update Database
                            ↓
                       Rebuild UI
```

## 🎓 المراجع - References

- **map.md**: شرح نظام التصنيف (Tagset)
- **README.md**: معلومات المشروع
- **PROCESSING_SUMMARY.md**: توثيق المعالجة

---

**تم الإنجاز**: 2025-11-11
**الإصدار**: v1.0
**الحالة**: ✓ مكتمل وجاهز للاستخدام
