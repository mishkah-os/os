# Treat broker media assets as binary to avoid diff/PR tooling errors.
# For the two-PR workflow (assets-only + code-only), see static/assets/broker/ASSETS_PR.md.
static/assets/broker/*.png binary
static/assets/broker/*.jpg binary
static/assets/broker/*.jpeg binary
static/assets/broker/*.webp binary

# Keep SVGs as text for reviewability (switch to binary if needed)
static/assets/broker/*.svg text eol=lf
