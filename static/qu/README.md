# محلل مورفولوجي عربي عتيق

## نظرة عامة

هذا المشروع يحتوي على تحليل مورفولوجي شامل لـ **14,808 كلمة عربية عتيقة** من القرآن الكريم والنصوص الكلاسيكية.

---

## 📂 هيكل المشروع

### المجلدات الرئيسية

```
qu/
├── batches/          # 15 ملف - الملفات المقسمة (الملف الأصلي)
│   ├── batch_01.json  (988 كلمة)
│   ├── batch_02.json  (988 كلمة)
│   └── ...
│
├── final/            # 15 ملف - الملفات المصححة يدويّاً
│   ├── final_01.json
│   ├── final_02.json
│   └── ...
│
├── map.md            # نظام التصنيف (Tagset)
├── sample2.json      # نموذج الملف المصحح
└── README.md         # هذا الملف
```

---

## 🔍 صيغة البيانات

### الملفات في مجلد `batches/`:

```json
[
  ["مِن", "م.ن"],
  ["اللّٰه", "#.ل.ه"],
  ["فِي", "ف.#"],
  ...
]
```

### الملفات في مجلد `final/` (المصححة):

```json
[
  ["مِن", "م.ن", "1.1"],
  ["ال+لّٰه", "ء.ل.ه", "1.5,2.2"],
  ["فِي", "ف.#", "1.1"],
  ...
]
```

---

## 📋 شرح الحقول الثلاثة

### الحقل الأول: الكلمة المقسمة (Morpheme Breakdown)

الكلمة مقسمة إلى وحدات صغيرة (مورفيمات) مفصولة بـ `+`:

- **مثال 1**: `مِن` (كلمة بسيطة)
- **مثال 2**: `ال+لّٰه` (حرف تعريف + اسم)
- **مثال 3**: `وَ+قال+ُوا` (حرف عطف + فعل + ضمير)

### الحقل الثاني: الجذر (Root)

الجذر الثلاثي أو الرباعي للكلمة:

- **ق.و.ل** - جذر (قال)
- **م.ن** - جذر (من)
- **#.ل.ه** - جذر مع علامات خاصة
- **NTWS** - اسم أعجمي بدون جذر ثلاثي

### الحقل الثالث: التصنيفات (Tags)

رموز رقمية تحدد نوع كل مورفيم (مفصولة بفواصل):

```
1.1  → حرف جر
1.2  → حرف عطف
1.10 → حرف نفي
2.1  → اسم عام
2.2  → اسم علم
2.3  → اسم إشارة
2.4  → اسم موصول
3.1  → فعل ماضٍ
3.2  → فعل مضارع
4.1  → ضمير منفصل
4.2  → ضمير متصل
5.1  → ظرف زمان
5.2  → ظرف مكان
```

انظر `map.md` للتصنيفات الكاملة.

---

## 📚 أمثلة

### مثال 1: حرف جر بسيط

```json
["مِن", "م.ن", "1.1"]
```

- الكلمة: مِن
- الجذر: م.ن
- التصنيف: 1.1 (حرف جر)

### مثال 2: اسم معرّف

```json
["ال+لّٰه", "ء.ل.ه", "1.5,2.2"]
```

- الكلمة: مقسمة إلى (ال + لّٰه)
- الجذر: ء.ل.ه
- التصنيفات:
  - 1.5 (حرف تعريف)
  - 2.2 (اسم علم)

### مثال 3: فعل مع ضمير

```json
["قال+ُوا", "ق.و.ل", "3.1,4.2"]
```

- الكلمة: مقسمة إلى (قال + ُوا)
- الجذر: ق.و.ل
- التصنيفات:
  - 3.1 (فعل ماضٍ)
  - 4.2 (ضمير متصل)

### مثال 4: حرف عطف + نفي

```json
["وَ+لا", "ل.#", "1.2,1.10"]
```

- الكلمة: مقسمة إلى (وَ + لا)
- الجذر: ل.#
- التصنيفات:
  - 1.2 (حرف عطف)
  - 1.10 (حرف نفي)

---

## 🛠️ الأدوات المستخدمة

تم استخدام عدة أدوات Python لتحليل ومراجعة الكلمات:

1. **process_words.py**: تقسيم الملف الأصلي
2. **morphological_analyzer.py**: تحليل مورفولوجي أولي
3. **advanced_corrector.py**: تصحيح متقدم
4. **detailed_manual_corrector.py**: تصحيح مفصل
5. **manual_line_by_line_analyzer.py**: محلل يدوي
6. **smart_rule_based_analyzer.py**: محلل ذكي
7. **meticulous_manual_review.py**: مراجع يدوي دقيق

---

## 📖 نظام التصنيف (Tagset)

يرجى الرجوع إلى ملف `map.md` للحصول على القائمة الكاملة بـ:

- **1.x**: الحروف (جر، عطف، نفي، جزم، إلخ)
- **2.x**: الأسماء (عام، علم، إشارة، موصول)
- **3.x**: الأفعال (ماضٍ، مضارع، أمر)
- **4.x**: الضمائر (منفصل، متصل)
- **5.x**: الظروف (زمان، مكان)
- **6.x**: متفرقات
- **7.x**: الصفات

---

## 💡 حالات الاستخدام

### 1. البحث اللغوي

ابحث عن جميع استخدامات حرف معين أو جذر:

```bash
# ابحث عن جميع استخدامات حرف الجر "من"
grep '"مِن"' final/*.json

# ابحث عن جميع الأفعال الماضية (3.1)
grep '"3.1"' final/*.json
```

### 2. التحليل الإحصائي

احسب تكرار التصنيفات المختلفة:

```python
import json

for i in range(1, 16):
    with open(f'final/final_{i:02d}.json') as f:
        data = json.load(f)
    for entry in data:
        # معالجة البيانات...
```

### 3. المعالجة اللغوية الطبيعية (NLP)

استخدم البيانات لتدريب نماذج اللغة:

- تحليل بناء الجملة
- كشف الأخطاء الإملائية
- توليد النصوص

---

## 📊 الإحصائيات

- **إجمالي الكلمات**: 14,808
- **عدد الملفات**: 30 (15 batch + 15 final)
- **متوسط الكلمات لكل ملف**: 987
- **صيغة البيانات**: JSON

---

## ⚙️ المتطلبات

- Python 3.6+
- مكتبة `json` (مدمجة في Python)

---

## 🚀 البدء السريع

### 1. عرض محتوى ملف

```bash
python3 -c "import json; print(json.dumps(json.load(open('final/final_01.json'))[:5], ensure_ascii=False, indent=2))"
```

### 2. إحصاء عدد الكلمات

```bash
python3 << 'EOF'
import json
total = 0
for i in range(1, 16):
    with open(f'final/final_{i:02d}.json') as f:
        total += len(json.load(f))
print(f"إجمالي الكلمات: {total}")
EOF
```

### 3. البحث عن حرف معين

```bash
python3 << 'EOF'
import json
for i in range(1, 16):
    with open(f'final/final_{i:02d}.json') as f:
        data = json.load(f)
    for entry in data:
        if 'قال' in entry[0]:
            print(entry)
EOF
```

---

## 📝 الملاحظات

1. **الحروف العربية**: تأكد من استخدام ترميز UTF-8
2. **الحركات**: بعض الملفات قد تحتوي على حركات عربية
3. **الترتيب**: الملفات مرتبة بترتيب ظهور الكلمات في الملف الأصلي

---

## 📖 المراجع

- نظام التصنيف: انظر `map.md`
- مثال كامل: انظر `sample2.json`
- توثيق العملية: انظر `PROCESSING_SUMMARY.md`

---

## 👤 المساهمون

تم إنشاء هذا المشروع كجزء من مشروع تحليل مورفولوجي شامل للعربية.

---

## 📄 الترخيص

هذا المشروع مفتوح المصدر. يرجى الرجوع إلى ملف الترخيص في المستودع.

---

**تم الإنجاز**: 2025-11-11
**الحالة**: ✓ مكتمل

