# توثيق مكتبة مشكاة (Mishkah Framework Documentation)

مشكاة هو إطار عمل ثوري لتطبيقات الويب، يجمع بين قوة الأداء (VDOM) وبساطة التطوير (Pure Vanilla JS). صُمم ليعيد للمطورين السيطرة الكاملة بدون تعقيدات أدوات البناء (Build Tools) أو طبقات التعقيد الإضافية.

## 🌟 الفلسفة والمزايا (The Genius System)

يعتمد النظام على مبدأ "العبقرية في البساطة". بدلاً من سلاسل التوريد المعقدة (Webpack/Vite/Babel)، تعمل مشكاة مباشرة في المتصفح، مع الحفاظ على قدرات أطر العمل الحديثة.

### 1. صفر أدوات بناء (Zero Build Step)
- لا حاجة لـ `npm install` أو `npm run build` للبدء.
- يعمل الملف مباشرة في المتصفح عبر استدعاء `mishkah.js`.
- وداعاً لمشاكل التوافقية (Dependency Hell).

### 2. تقنية HTMLx
- تحويل ملفات HTML العادية إلى تطبيقات تفاعلية ذكية.
- كتابة المنطق (`<script>`)، والحالة (`data-m-path`)، والواجهة (`template`) في ملف واحد منظم.
- دعم القوالب الديناميكية والمتغيرات التفاعلية `{{state.value}}`.

### 3. مركزية VDOM (Virtual DOM Central)
- محرك VDOM قوي وسريع جداً مدمج في النواة (`mishkah.core.js`).
- تحديثات جراحية للواجهة (Surgical Updates) تضمن أعلى أداء.
- خوارزميات متقدمة لمقارنة القوائم (List Diffing).

### 4. جاهزية للخادم (SSR Ready)
- بما أن المكتبة مكتوبة بـ Pure JS ومتوافقة مع معايير V8، يمكن تشغيلها على الخادم (Node.js) لتقديم Server-Side Rendering قوي جداً بدون تعديلات.

### 5. نظام تصميم مدمج (Built-in Design System)
- مكتبة `mishkah-ui.js` توفر مكونات جاهزة (Cards, Buttons, Modals) مبنية على نظام Tokens مرن.
- دعم مدمج لـ Tailwind CSS أو CSS مخصص.

---

## 📚 دليل الملفات (Library Reference)

فيما يلي توثيق شامل لمحتويات مكتبة `@lib` ودور كل ملف في المنظومة:

### 🛠️ النواة والأساس (Core & Boot)

#### `mishkah.js`
**المحمل الآلي (The Auto Loader):**
- هو نقطة الدخول الوحيدة للإطار.
- يقوم بقراءة إعدادات `data-htmlx` و `data-config` من وسم السكريبت.
- يتحقق من وجود المكتبات المطلوبة ويقوم بتحميلها بشكل تسلسلي ذكي (Sequential Loading).
- يهيئ بيئة التطبيق (`Mishkah.app`) ويطلق إشارة البدء.

#### `mishkah.core.js`
**المحرك الرئيسي (The Engine):**
- يحتوي على تطبيق الـ Virtual DOM بالكامل (`h`, `patch`, `render`).
- يدير دورة حياة التطبيق (App Lifecycle).
- يوفر نظام DSL (Domain Specific Language) لبناء العناصر (`D.Containers.Div`, `D.Text.H1`).
- يدير نظام الأحداث (Event Delegation) المركزي.

#### `mishkah-htmlx.js`
**عميل التفاعل (The Reactive Agent):**
- المسؤول عن قراءة وسوم `<template>` وتحويلها إلى تطبيق حي.
- يعالج توجيهات البيانات مثل `data-m-path` لربط الحالة (State) بالواجهة.
- ينفذ منطق القوالب (`{{variable}}`) ويعالج التحديثات التفاعلية.
- يربط الأحداث (`onclick`) بالوظائف المعرفة في السكريبت.

### 🎨 الواجهة والتصميم (UI & Design)

#### `mishkah-ui.js`
**مكتبة المكونات (Component Library):**
- تحتوي على نظام Tokens للتصميم (الألوان، المسافات، الخطوط).
- توفر مجموعة غنية من المكونات الجاهزة عالية الجودة:
  - `UI.Button`, `UI.Card`, `UI.Modal`, `UI.Toolbar`.
- تتضمن جسر `ChartBridge` لدمج رسوم `Chart.js` البيانية بسهولة.

#### `mishkah-css.css`
**محرك التنسيق (Styling Engine):**
- ملف CSS الأساسي الذي يوفر المتغيرات (Variables) والأنماط الافتراضية.
- يدعم الوضع الليلي (Dark Mode) والاتجاه (RTL/LTR) بشكل تلقائي.

#### `mishkah.templates.js`
**القوالب الجاهزة (Ready-made Layouts):**
- يوفر تخطيطات كاملة للصفحات (Layouts) مثل `Aurora`, `Atlas`, `Zen`.
- يسرع عملية بناء لوحات التحكم وصفحات الهبوط (Landing Pages).

### 🔧 أدوات مساعدة (Utilities & Extensions)

#### `mishkah.utils.js`
**صندوق الأدوات (Toolbox):**
- دوال مساعدة للتعامل مع الشبكة (Network)، التخزين (Storage)، والمصفوفات.
- أدوات معالجة البيانات والوقت.

#### `mishkah.store.js`
**إدارة الحالة المتقدمة (State Management):**
- (اختياري) يوفر نمط `Store` لإدارة الحالة المعقدة في التطبيقات الكبيرة.
- يدعم الـ Middlewares والـ Actions المنظمة.

#### `mishkah-ui-devtools.js`
**أدوات المطور (Developer Tools):**
- تساعد في فحص حالة التطبيق، تتبع الأداء، وتصحيح الأخطاء أثناء التطوير.

---

## 🚀 دراسة حالة: `test.html`

ملف `test.html` يمثل النموذج المثالي (The Golden Standard) لبناء تطبيق بمشكاة.

### الهيكل العام:
1. **التعريف:** `<html data-htmlx="main">` يخبر المشكاة أن التطبيق يبدأ من القالب المسمى `main`.
2. **الاستدعاء:** `<script src="../lib/mishkah.js" ...>` يحمل الإطار.
3. **القالب:** `<template id="main">` يحتوي على كل شيء:
   - **البيانات:** `<script data-m-path="data">` لتعريف الحالة الأولية.
   - **الواجهة:** HTML مع روابط تفاعلية `{state.data.count}`.
   - **المنطق:** `<script>` في النهاية يحتوي على دوال `increment`, `setTheme`.

### لماذا هذا النظام عبقري؟
- **التماسك (Locality of Behavior):** البيانات، الواجهة، والمنطق في مكان واحد منطقي.
- **الأداء:** يتم تحويل هذا القالب النصي إلى VDOM Functions عالية الكفاءة.
- **البساطة:** يمكن لأي مطور HTML/JS فهمه وتعديله فوراً بدون تعلم Syntax غريب.

---

> **الخلاصة:** مشكاة ليست مجرد مكتبة، بل هي عودة لجذور الويب القوية مع قدرات العصر الحديث. هي الخيار الأمثل لمن يريد بناء أنظمة قوية، سريعة، وقابلة للصيانة لعقود قادمة.
